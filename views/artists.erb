<h2>Artists</h2>

<table class="table table-striped">
  <tr>
    <td>
      Name
    </td>
  </tr>

  <% @artists.each do | artist | %>
  <tr>
    <td>
      <a href="/artists/<%= artist.id %>"><%= artist.name %></a>
    </td>
  </tr>
  <% end %>

</table>
